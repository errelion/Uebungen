package AuD;

/**
 * Created by Benjamin Fani Sani on 30.11.16.
 */
public class linkedList {
/**
    a LinkedList

    algorithm a.insert(it, x){
        el = neues Listenelement;
        el.value = x;
        el.prev=a(it).prev;
        a(it).prev=el;
        el.next=a(it);
    }

    algorithm a.delete(it){
        el = a.value(it);
        a(it) freigeben;

        itNext=a.next(it);
        it=a.prev(it);
        a(it).next=a(itNext);
        a(itNext).prev=a(it);

        return el;
    }
*/
    public static void main(String[] args) {

    }
}
